# 2026-08-23

## 修复测试异常（7 passed）

- **DB 异常**：迁移 `c1f29a4b9156` 已从 task 表删除 `lease_owner`/`lease_expire_at`，但 `app/storage/mysql/models.py` 的 Task 模型仍保留这两个字段，导致 INSERT 报 `Unknown column 'lease_owner'`（线上 API POST /tasks 也因此 500）。按 MVP「无 lease/heartbeat」方向，删除了模型中的这两个字段。
- **pytest 收集错误**：`tests/test_api_e2e.py` 原为脚本式写法（测试函数带 task_id/timeout_s 参数），改造为 pytest fixture 结构（`api_server` 不可达时 skip、`created_task` module 级 fixture 共享创建的任务）。
- **修正 SSE 测试中 `_process_task` 调用**：worker 签名为 2 参 `(task_id, worker_id)`，原测试传了 3 参（多传 settings）。
- 清理了测试函数返回值触发的 PytestReturnNotNoneWarning。
- 重启了本地 8000 端口的 uvicorn 服务以加载修复后的模型。
